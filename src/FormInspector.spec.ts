import { FormInspector } from "./index";
describe('FormInspector', () => {
    it('should create instance', () => {
        const inspector = new FormInspector();
        expect(inspector).toBeTruthy();
    });
});